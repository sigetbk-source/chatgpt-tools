# 共通エージェント指示

このファイルは特定の LLM に依存しない共通ルールです。

## 探索順序

1. ルート `README.md` とこの `AGENTS.md` を読む。
2. 対象の `skills/<name>/README.md` または `projects/<name>/README.md` を読む。
3. 対象ディレクトリに `AGENTS.md`、`SKILL.md`、仕様書、開発ルールがあれば、作業に必要なものを読む。
4. GitHub 操作では `docs/github-operations.md`、LLM 間の役割分担では `docs/llm-usage.md` を確認する。

## 編集ルール

- 変更範囲を小さく保ち、別の skill / project を巻き込まない。
- 編集前に実ファイル、Git の状態、正本となるブランチを確認する。
- 重要な仕様変更では、実装と同じ変更単位で README、仕様書、運用文書、開発ログ、CHANGELOG を更新する。
- 既存コード、テンプレート、ロゴなどの固定資産を優先して再利用し、理由なく作り直さない。
- 未検証の動作を「完了」「テスト済み」「実機確認済み」と記載しない。
- 既存ルールを無断で削除または弱体化しない。

## Codex サブエージェント運用

Codex では `.codex/config.toml` と `.codex/agents/*.toml` を役割定義の正本とする。親エージェントは全体判断と最終統合を担当し、必要なときだけサブエージェントへ委譲する。

- 小さな単一ファイル修正や、親だけで短時間に終わる作業ではサブエージェントを増やさない。
- 読み取り中心の探索、ファイル列挙、定型チェックは `explorer_lite` を使う。
- 仕様が明確な通常実装、既知パターンの修正、限定されたテストは `worker` を使う。
- 複雑なロジック、回帰リスク、セキュリティ、重要なレビューは `reviewer` を使う。
- Browser / Computer Use を多用する作業は `browser_operator` を使う。`browser_operator` は親モデルを継承するため、Astra など UI 操作に強いモデルを親で選んだ場合はそのまま活かす。
- 同じ状態を持つブラウザや GUI を複数の操作エージェントから同時に触らせない。
- 並列化は独立性が高い読み取り作業を優先し、書き込みエージェントの同時起動は必要最小限にする。
- サブエージェントの結果は親が検証してから採用し、相互に矛盾する変更をそのまま統合しない。
- 高価なモデルは曖昧な設計判断、難しいデバッグ、Browser / Computer Use、最終レビューへ優先配分し、定型処理では下位モデルを使う。

## 生成物と一時ファイル

- ビルド生成物、キャッシュ、ローカル作業用ファイル、自動再生成できる中間成果物はコミットしない。
- 完成サンプルを管理する必要がある場合は、用途を README に明記する。
- パスワード、トークン、証明書、署名データ、個人情報、ユーザー固有の IDE 設定をコミットしない。

## リポジトリ境界

- 新しい分野や小規模ツールは、まずこのリポジトリ内の `skills/` または `projects/` への追加を検討する。
- 公開範囲、権限、CI/CD、リリース、履歴、ライセンスを分離する明確な理由がない限り、勝手に新規リポジトリへ分割しない。
- 既存リポジトリを集約しても、移行元を勝手に削除・アーカイブ・変更しない。
